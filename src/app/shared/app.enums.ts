export enum Role {
  Member = 1,
  Officer = 2,
  Founder = 3
}

export enum EntityCollection {
  None = 0,
  Image = 1,
  Biography = 2,
  All = Image + Biography
}

export enum PageType {
  Mission = 1,
  Readings = 2,
  Sessions = 3,
  CustomPage = 4
}

export enum NotificationsType {
  WelcomeMessage = 1
}
